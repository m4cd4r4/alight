node_modules/
dist/
.vite/
*.tsbuildinfo
.vercel/
coverage/
*.log
.env
.env.*
!.env.example
.DS_Store
Thumbs.db

# Vendored piano samples - fetched at build (scripts/fetch-piano-samples.mjs),
# kept out of git history. Drop this line + `git add public/samples` to commit them.
public/samples/

# Local browser-verification screenshots + scripts (regenerated on demand, not committed)
docs/verification/
verify-shots/
.vercel
.env*.local
